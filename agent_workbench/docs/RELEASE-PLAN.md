# 0.4.13 Documentation and Release Plan

Scope approved: rebuild the standard Windows installer; publish an interview-oriented README and real native-UI screenshots. No GitHub push or change to the user's installed application.

1. Bump release version and preserve accepted 0.835/0.013 defaults.
2. Run the standard build, native regression and packaged restart/tray checks.
3. Capture every principal surface in a separate native instance using synthetic public demo content; no real provider credentials, personal histories or local user documents.
4. Document architecture, stack, reproducible setup, feature boundaries, evaluation evidence and interview walkthrough.
5. Prepare a source-only GitHub archive with private files, build products, environments and oversized weights excluded. Verify model download and source layout.
6. Review screenshots, links, archive contents, secrets and final artifact hashes before delivery.

Completed: version/build, 318 Python and 43 frontend tests, 34-check native rerun, 15 packaged checks, 19 screenshots, source export and isolated extraction checks. The first native access-denied failure is retained in RELEASE-0.4.13.md. No GitHub push or user installation change was performed. The owner subsequently chose MIT for original code; the license and source archive were updated without rebuilding the installer.
